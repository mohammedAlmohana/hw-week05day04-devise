<%= form_with(model: cars1, local: true) do |form| %>
  <% if cars1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cars1.errors.count, "error") %> prohibited this cars1 from being saved:</h2>

      <ul>
        <% cars1.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
